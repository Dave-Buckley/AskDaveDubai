---
import PageLayout from './PageLayout.astro';
interface Props {
  title: string;
  description: string;
  ogImage?: string;
}
const props = Astro.props;
---
<PageLayout {...props}>
  <slot name="head" slot="head" />
  <slot />
</PageLayout>
